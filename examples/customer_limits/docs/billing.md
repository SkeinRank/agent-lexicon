# Billing terminology

Customer cap is the account-level credit limit. Billing agents should use the
`billing.update_credit_limit` tool when a scoped request clearly refers to the
credit limit.

Manual billing audit may review a credit limit update without calling the scoped tool.
